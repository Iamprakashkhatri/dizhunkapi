from django.conf.urls import url
# from .views import UserRegisterAPIView,UserLoginAPIView,LogOutAPIView

app_name="accounts"

urlpatterns = [
    # url('register/',UserRegisterAPIView.as_view()),
    # url('login/',UserLoginAPIView.as_view()),
    # url('logout/',LogOutAPIView.as_view()),
]